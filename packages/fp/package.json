{
  "name": "@mz3/fp",
  "files": [
    "dist/"
  ],
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest -c ../../jest.unit.js $PWD",
    "build": "tsc -b -v",
    "clean": "tsc -b --clean",
    "prepare": "yarn clean; yarn build"
  },
  "dependencies": {
    "ramda": "^0.26.1"
  },
  "devDependencies": {
    "@types/ramda": "^0.26.38"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mz3/code.git"
  }
}
